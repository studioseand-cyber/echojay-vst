{
  "_note": "M9 fixture: SSL X-Gate. The fixture CARRIES ITS LADDER POINTS so a regression run is reproducible. The generic chooser runs only when nothing pins them -- measured on eq, where a centred 2-octave pair lands at 54.1/216.3 Hz against the fixture's 100/400, so a chooser-driven run cannot reproduce a pinned run's numbers and is not a regression test.",
  "subject_id": "AudioUnit:Effects/aufx,XGAT,SSLN",
  "subject_name": "SSL X-Gate",
  "semantic": "threshold_db",
  "ladder_points": [-10.80, -28.80, -46.80, -64.80],
  "points_origin": "the 0.15/0.4/0.65/0.9 fractions of the live 0..-72 ladder, as measured on 3 Aug 2026 before parameterisation; pinned here so the parameterised suite is compared against the same stimulus"
}
