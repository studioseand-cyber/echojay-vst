# THE MERGE, 6 Sep 2026 - Kathy's work into the integration branch, before the shoot

Order changed by Sean: he wants Kathy's work in the plugin he films with.
Gate 1 (both machines fully pushed) verified by ls-remote before anything
moved: local HEAD 7399ed8 == remote backup/seand-mac-integration-2026-09-06,
0 unpushed here; her tip aa455ff on origin/integration/reasoning-plus-pitch.
Gate 2 withdrawn. Branch: merge/kathy-2026-09-06 (from 7399ed8).

## The two stages (back-to-back, one build)
    0075fe8  stage 1: d68da09..f6ac073   ONE conflict - predicted #1 (ChainHost.cpp include block).
             It arrived at THIS stage, not stage 2 as the survey read; the hunk was the
             predicted one to the line. Resolved as written: all six includes kept.
    9177b96  stage 2: f6ac073..aa455ff   ONE conflict - predicted #2 (PluginEditor.cpp settings
             movers list). Resolved as written: her &dialWritesToggle kept, our removal of
             &settingsWithheldToggleBtn_ kept. Predicted #3 (EedDeviceProcessor.cpp restore
             sequence) AUTO-MERGED with onStateApplied() intact after applyParams (lines 243-247:
             applyingState_ = true; resetParamsToDefaults(); applyParams(...); applyingState_ =
             false; onStateApplied();). V7 confirms the migration survived (below).
    NO unpredicted conflict. Nothing shown to Sean for a decision.
    Files the merge touched under Source/Eed* and Source/EJ*: her six new EJ*.h headers and
    EedDeviceProcessor.cpp (+15, the guard). NO pitch engine file (EedPitch*, EedPsola*,
    EedRetuneMap) changed: every pitch constant is identical by construction, and read back anyway.

## The build (build-release, -j 4, explicit targets, logged)
    EchoJay_AU EchoJay_VST3 EchoJayLink_AU EchoJayLink_VST3 EchoJayPitchModeTest: exit 0,
    0 errors, 616 warnings (196 PluginEditor.cpp, 147 ChainHost.cpp, 110 PluginProcessor.cpp -
    the pre-existing bulk; 4 in her new EJ*.h files).
    arm64 UUIDs of 9177b96 (the first build, BEFORE the V3 fix below):
      EchoJay V2 AU 9691505F-8EA7-3B02-ACCE-213F1FC20522   VST3 9AABF5EA-80BE-3D6B-8946-BE9D20012610
      Link AU       11F62328-8B13-36C7-9992-AD3E13075F95   VST3 5AAE46FF-545C-3A24-ACEE-2DD9B0327C3F

## GATING SET (all must pass before anything replaces the installed F03A4363)
| leg | instrument | result |
|---|---|---|
| build clean | the log above | PASS: 0 errors |
| AU and VST3 both load | tools/merge_gate_tests/plugin_load_test.mm - hosts the BUILT bundles from build-release WITHOUT installing (VST3 by path; AU registered in-process from the bundle's plist + factory, then hosted through the same JUCE path) | PASS x4: EchoJay V2 AU + VST3, EchoJay Link AU + VST3 all instantiate at 48k/512 |
| plugin passes audio | same harness: 16 blocks of a 440 Hz sine at 0.1768 rms | PASS x4: output rms 0.1767, finite; latency 0 (empty chain). Also tools/latency_impulse_test through PluginProcessor: every impulse arrives where reported |
| pitch suite | build-release/EchoJayPitchModeTest_artefacts/Release/EchoJayPitchModeTest | PASS: 196 PASS / 0 FAIL, "ALL PASS (0 failures)" (log: tools/merge_gate_tests/results_2026-09-06/pitch_suite_9177b96.log) |
| DERIVED_VALUES_SINCE_BASE.md read-back | grep of 9177b96 | PASS, every row: kDefRetuneMs 6, kRetuneFloorMs 6, kMaxRetuneMs 150, kNoteConfirmMs 15, kNoteChangeCents 90 (noteChangeC_ = kNoteChangeCents), kGapIsNoteChangeMs 200, kSeamFadeMs 1.5 (EedPsolaEngine.h:143), kBorrowAlignBudgetFrames 16384 (PluginProcessor.h:457), noteDecExp_ 0, natVib_ literal 100 (as recorded, B8 held); schema: correction_mode default kCustom, retune 0-400 default 0, retune_speed_ms INTERNAL default kDefRetuneMs, seam_attack_ms 60, depth 100, targeting_ignores_vibrato 1, natural_vibrato 0, flex 0, humanize 0; kPresets 120/55/60/100, 40/25/30/100, 8/0/0/40, 0/0/0/0, ignoreVib true x4; EedRetuneMap 6 + 74 t^2, 1 - 0.65 t^0.7, anchors (50: 80, .35) (100: 150, .25) (200: 150, .15) (400: 150, .10); key setters guarded by ! applyingState(). Plus: the diff 7399ed8..9177b96 touches none of these files |
| V8 explicit ParamSource at every applyParams/applyStructured call | grep of 9177b96 | CANNOT PASS AS WRITTEN: there is NO ParamSource in aa455ff. Her fix (a required ParamSource making the guard inert on the restore path) is not in her pushed tip; the guard `if (echojay::dialWritesBlocked()) return {};` sits at EedDeviceProcessor.cpp:102 inside applyParams, and setStateInformation calls applyParams at :245 with no source argument. Call sites listed for when it lands: EedDeviceProcessor.cpp:80 (applyStructured delegating), :245 (setStateInformation - THE RESTORE PATH); EedPitchProcessor.cpp:637, :647; EedMultibandProcessor.cpp:338, :374; SurgicalEqProcessor.cpp:559; SurgicalEqEditor.cpp:207, :258; ChainHost.cpp:3406; tools/pitch_host_test/main.cpp:48, :113, :268; tools/pitch_mode_test/main.cpp:56 |
| V3 Settings renders, her toggle present, our block gone, nothing clipped | tools/settings_snapshot at 900x580, 1000x650, 1100x720, 1400x900, 1800x1200 (tools/settings_snapshot/snapshots_2026-09-06/merge_9177b96/) | her toggle PRESENT at every size ("Suggest settings but never dial them (you set the values by hand)", directly beneath the auto-dial toggle); WITHHELD block GONE; bounds audit 0 failures; chain feed 2328 / AU view 2129 IDENTICAL to the shoot build. BUT: THE RENDER SHOWS THE "YOUR PLUGINS" HEADING HALF-HIDDEN BEHIND HER TOGGLE at every size. Cause (read in both trees): resized() steps the layout cursor past the second toggle (PluginEditor.cpp:19477 `sy += fh + 8`), paintSettingsView does not - the heading is drawn at the y the auto-dial toggle alone would leave. PRESENT IN HER TREE aa455ff (same paint code at her line 14586): her defect, pre-existing, not made by the merge; V3 is what caught it. Fixed on the merge branch as its own commit: ONE line in the paint cursor (`y += fh + 8;` after label("CHAIN SUGGESTIONS")), re-rendered - see the V3 AFTER row below. One-name delta noted: enabled names resolving in NEITHER format 540 vs 539 on the shoot build (her resolver/feed changes are in this tree); the feed itself is identical |

## RUN, NOT GATING
| leg | result |
|---|---|
| V1 transport reset positive control + bit-identity | PASS inside the suite: the TRANSPORT RESET block (positive control differs, the reset renders bit-identical to a fresh instance) |
| V2 five-state impulse table + quiet session | PASS, tools/latency_impulse_test: (a) 0/0, (b) pending 0/0, (b') unknown transport pending 0/0, (c) committed at STOP 16384/16384 with exactly one notification, (d) pending-down 16384/16384 no notification, (e) committed-down 0/0 one notification; prepare re-decision 16384; L7 quiet session 1200 blocks reported 0 zero notifications; L4 scoping 8/8. ALL PASS (0 failures). (auth.json mtime changed during the run - the known network poll, as before) |
| V4 status line / numbers box / grid provenance snapshots | PASS: all seven EJ_EDITOR_SNAP renders BYTE-IDENTICAL to tools/pitch_mode_test/snapshots_2026-09-05/ (cmp) |
| V5 explicit WetSource at every setSlotWet | 6 of 9 explicit. THREE implicit (default = Assistant): ChainHost.cpp:1956, :2134, PluginEditor.cpp:30675 - all three from 1bb9cd0 (Sean, 16 Aug, IN THE BASE d68da09): pre-existing, not merge-introduced, the assistant-op paths where Assistant is the intended source. Listed, not changed |
| V6 two independent history limits + a real request | constants PASS: maxHistoryMessages = 12 (EchoJayAPI.cpp:1099) and maxHistoryBytes = 24000 (:1113), both passed at :1146. The real request NOT RUN: this harness environment has no logged-in session (/api/me answers 404 "User not found" in every harness run today) - to be observed in Sean's Logic session |
| V7 reference provenance (the leg that catches a wrong #3) | PASS inside the suite: "laundered state (manual + value, no marker) reverts to AUTO on load", "marked manual 442 survives a save/load round-trip", "auto survives a save/load round-trip", and SEAN'S SAVED STATE - all pass; onStateApplied() is in the merged restore sequence |
| V9 saved values survive a reload with do-not-dial ON | FAIL - AS THE PRE-FIX CODE PREDICTS. tools/merge_gate_tests/v9_dialwrites_restore_test.cpp: positive control (setting OFF) PASS, all five values back; setting ON: retune 200 -> 0, retune_speed_ms 150 -> 6, natural_vibrato 50 -> 0, targeting_ignores_vibrato 0 -> 1, flex 30 -> 0: DATA LOSS, defaults written, nothing restored. This is Kathy's find, reproduced on the merged tree, and it becomes the regression test that must pass once her fix lands. EXPOSURE ON SEAN'S MAC: the setting persists in ~/.echojay/dev.json as "dialWritesBlocked"; the key is ABSENT there today (=> false; the toggle is new to this Mac). The loss fires only with the toggle ON at the moment a session/device state is restored |

## V3 AFTER the one-line paint fix (its own commit on the merge branch)
    Rebuilt EchoJay_AU + EchoJay_VST3 only (Link does not compile PluginEditor.cpp), -j 4, exit 0, 0 errors.
    tools/settings_snapshot/snapshots_2026-09-06/merge_v3fix/: her toggle present, our block gone,
    YOUR PLUGINS heading now clear between her toggle and Scan Plugins at 900x580, 1000x650,
    1100x720, 1400x900, 1800x1200 (inspected at 900x580 and 1400x900); bounds audit 0 failures;
    counts unchanged (2328 / 2129 / 540). Load gate re-run on the rebuilt bundles: both formats
    load and pass audio. V3 PASSES.
    arm64 UUIDs OF THE CANDIDATE BUILD (the one an install would write):
      EchoJay V2 AU   3A0CDE7C-B3CA-3CF9-96C1-57F2233E7DAB
      EchoJay V2 VST3 D10C962C-98C9-39FC-9371-B705F1F6422A
      EchoJay Link AU 11F62328-8B13-36C7-9992-AD3E13075F95   (unchanged from the first build)
      EchoJay Link VST3 5AAE46FF-545C-3A24-ACEE-2DD9B0327C3F (unchanged)

## Install decision
NOT INSTALLED by this pass. The gating set is green EXCEPT V8, whose subject (the ParamSource
fix) is not in aa455ff - it cannot be run, only its consequence (V9's data loss with the toggle
ON) can be shown; that is a ruling for the reviewer, not a resolution for this pass. F03A4363 stays
installed; ~/ej-installed-backup-shoot/REVERT.command is the fallback. The install line, when
ruled: `tools/install_local.sh build-release` (never /Library, never reinstall-v2.sh), then kill
AUHostingService and relaunch Logic; UUIDs to verify are printed by the script (arm64 line).
